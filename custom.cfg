MEMORY {
  ZP: start = $0000, size = $0100;
  RAM: start = $0200, size = $7E00;
  ROM: start = $8000, size = $7FFC;
}

SEGMENTS {
  ZEROPAGE: load = ZP, type = zp;
  CODE: load = ROM, type = ro;
  VECTORS: load = ROM, start = $FFFA;
}
